基于对各个网络原始训练代码的分析，我将调整`run_few_channels_experiments.sh`脚本中的超参数配置，使其与原始跨个体实验保持一致。具体调整如下：

### 1. ACRNN

* **原始配置**：batch\_size=16, epochs=1000, lr=0.001/10e-4

* **当前配置**：batch\_size=16, epochs=200, lr=0.0001

* **调整**：将epochs从200改为1000

### 2. BiDANN

* **原始配置**：batch\_size=16, epochs=80, lr=0.00004

* **当前配置**：batch\_size=16, epochs=10, lr=0.00004

* **调整**：将epochs从10改为80

### 3. DBN

* **原始配置**：batch\_size=512, epochs=100, lr=0.02

* **当前配置**：batch\_size=64, epochs=5, lr=0.02

* **调整**：将batch\_size从64改为512，epochs从5改为100

### 4. DGCNN

* **原始配置**：batch\_size=16, epochs=150, lr=0.0015

* **当前配置**：batch\_size=16, epochs=10, lr=0.0015

* **调整**：将epochs从10改为150

### 5. EEGNet

* **原始配置**：batch\_size=256, epochs=100, lr=0.001

* **当前配置**：batch\_size=16, epochs=50, lr=0.001

* **调整**：将batch\_size从16改为256，epochs从50改为100

### 6. FBSTCNet

* **原始配置**：未明确找到跨个体实验的配置

* **当前配置**：batch\_size=4, epochs=10, lr=0.001

* **调整**：batch\_size修改为32，epoch修改为150

### 7. R2GSTNN

* **原始配置**：batch\_size=32, epochs=60, lr=0.00002

* **当前配置**：batch\_size=8, epochs=5, lr=0.00002

* **调整**：将batch\_size从8改为32，epochs从5改为60

### 8. RGNN

* **原始配置**：batch\_size=32, epochs=150, lr=0.001/0.003

* **当前配置**：batch\_size=1, epochs=150, lr=0.001

* **调整**：将batch\_size从1改为32

### 9. SVM

* **原始配置**：未明确找到跨个体实验的配置

* **当前配置**：batch\_size=16, epochs=50, lr=0.001

* **调整**：保持不变

### 10. TSception

* **原始配置**：batch\_size=32, epochs=200, lr=0.001

* **当前配置**：batch\_size=16, epochs=5, lr=0.001

* **调整**：将batch\_size从16改为32，epochs从5改为200

通过这些调整，少通道实验的超参数配置将与原始跨个体实验保持一致，确保实验结果的可比性和可靠性。
